.message{id: message.id}
  .sent-name
    = message.user.name
  .sent-day
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  - if message.content.present?
    .sent-naiyo
      = message.content
  = image_tag(message.image.url, class: "sent-image") if message.image.url.present?
